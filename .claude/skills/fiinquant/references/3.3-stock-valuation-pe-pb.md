---
name: Lấy dữ liệu định giá P/E, P/B theo mã chứng khoán
description: Lấy dữ liệu định giá P/E và P/B theo một hoặc nhiều mã chứng khoán trong khoảng thời gian xác định trong FiinQuantX.
---

# 3.3. Lấy dữ liệu định giá P/E, P/B theo mã chứng khoán

Mô tả cách sử dụng thư viện sau khi người dùng đã đăng nhập.

**Tên hàm:** `client.MarketDepth().get_stock_valuation(...)`

**Mục đích:**
Lấy dữ liệu định giá P/E và P/B của một hoặc nhiều mã chứng khoán theo khoảng thời gian xác định, phục vụ phân tích định giá và theo dõi biến động chỉ số định giá theo thời gian.

**Tham số:**

| Tên tham số | Mô tả | Kiểu dữ liệu | Mặc định | Bắt buộc |
|---|---|---|---|---|
| tickers | Danh sách mã được viết in hoa | str hoặc list |  | Có |
| from_date | Mốc thời gian lấy dữ liệu xa nhất | str |  | Có |
| to_date | Mốc thời gian lấy dữ liệu gần nhất | str | datetime.now() | Không |

**Kết quả trả về (Returns):**
- Dữ liệu dạng bảng hoặc danh sách bản ghi
- Class xử lý: `MarketDepth`
- Phương thức nhận dữ liệu: `get_stock_valuation()`
- Kết quả bao gồm dữ liệu định giá P/E và P/B theo từng mã chứng khoán và từng thời điểm giao dịch

**Thuộc tính dữ liệu:**

| Tên thuộc tính | Mô tả | Kiểu dữ liệu |
|---|---|---|
| ticker | Tên mã | str |
| timestamp | Thời gian giao dịch | str |
| pe | Chỉ số P/E | float |
| pb | Chỉ số P/B | float |

**Ví dụ sử dụng:**

```python
data = client.MarketDepth().get_stock_valuation(
    tickers=["HPG", "MWG"],
    from_date="2025-08-28",
    to_date="2025-09-03"
)
print(data)