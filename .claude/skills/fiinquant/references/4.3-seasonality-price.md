---
name: Hàm SeasonalityPrice
description: Phân tích và trực quan hóa tính mùa vụ của giá cổ phiếu theo tháng, hỗ trợ hiển thị bảng phần trăm thay đổi giá, độ lệch chuẩn, Sharpe Ratio theo tháng và so sánh Sharpe Ratio trung bình giữa nhiều mã trong FiinQuantX.
---

# 4.3. Hàm SeasonalityPrice

Mô tả cách sử dụng thư viện sau khi người dùng đã đăng nhập.

**Tên hàm:** `SeasonalityPrice(data, tickers)`

**Mục đích:**
Dùng để phân tích tính mùa vụ của giá cổ phiếu trong một khoảng thời gian dựa trên dữ liệu lịch sử. Hàm hỗ trợ:
- Tính phần trăm thay đổi giá theo tháng
- Vẽ heatmap mức sinh lời theo từng tháng và từng năm
- Tính trung bình, độ lệch chuẩn và Sharpe Ratio theo tháng
- So sánh Sharpe Ratio trung bình giữa nhiều mã cổ phiếu

**Tham số:**

| Tên tham số | Mô tả | Kiểu dữ liệu | Mặc định | Bắt buộc |
|---|---|---|---|---|
| data | Dữ liệu giá lịch sử, tối thiểu cần có các cột `ticker`, `timestamp`, `close` | pandas.DataFrame |  | Có |
| tickers | Một mã hoặc danh sách mã cổ phiếu cần phân tích | str hoặc list |  | Có |

**Kết quả trả về (Returns):**
- Khởi tạo object `SeasonalityPrice`
- Hỗ trợ các phương thức:
  - `monthly_seasonality()`: trả về bảng dữ liệu seasonality theo tháng
  - `plot_seasonality()`: hiển thị heatmap thay đổi giá theo tháng và biểu đồ Sharpe Ratio theo tháng cho 1 mã
  - `plot_average_sharpe()`: hiển thị biểu đồ so sánh Sharpe Ratio theo tháng và Sharpe Ratio trung bình của nhiều mã
- Kết quả trực quan được hiển thị dưới dạng biểu đồ Plotly

**Ví dụ sử dụng:**

```python
import pandas as pd
import numpy as np
from plotly import graph_objects as go
from plotly.subplots import make_subplots
from FiinQuantX import FiinSession

username = 'REPLACE_WITH_YOUR_USER_NAME'
password = 'REPLACE_WITH_YOUR_PASS_WORD'

client = FiinSession(
    username=username,
    password=password
).login()

class SeasonalityPrice:
    def __init__(self, data, tickers):
        self.data = data
        self.data['timestamp'] = pd.to_datetime(self.data['timestamp'])
        self.tickers = []
        if isinstance(tickers, str):
            self.tickers = [tickers]
        elif isinstance(tickers, list):
            self.tickers = tickers
        else:
            self.tickers = [tickers]

    def monthly_seasonality(self):
        if len(self.tickers) > 1:
            raise ValueError("Only one ticker is supported for monthly seasonality")
        
        filtered_data = self.data.copy()
        filtered_data.set_index('timestamp', inplace=True)

        monthly_returns = filtered_data['close'].resample('M').last().pct_change() * 100
        monthly_seasonality = pd.DataFrame()
        monthly_seasonality['Month'] = monthly_returns.index.month
        monthly_seasonality['Year'] = monthly_returns.index.year
        monthly_seasonality['Returns'] = monthly_returns.values

        return monthly_seasonality
    
    def plot_seasonality(self):  
        if len(self.tickers) > 1:
            print("Tickers: ", self.tickers)
            raise ValueError("Only one ticker is supported for monthly seasonality")
        
        monthly_seasonality = self.monthly_seasonality()
        pivot_table = monthly_seasonality.pivot(index='Year', columns='Month', values='Returns').sort_index(ascending=True)
        monthly_averages = pivot_table.mean(axis=0).values.reshape(1, -1)  
        monthly_stdev = pivot_table.std(axis=0).values.reshape(1, -1)
        monthly_avg_plus_stdev = monthly_averages + monthly_stdev
        monthly_avg_minus_stdev = monthly_averages - monthly_stdev
        monthly_Sharpe_ratio = monthly_averages / monthly_stdev
        annotations = []
        for i, row in enumerate(pivot_table.values):
            for j, value in enumerate(row):
                annotations.append(
                    dict(
                        x=['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun', 'Jul', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'][j],
                        y=pivot_table.index[i],
                        text=f'{value:.2f}' if not np.isnan(value) else '',
                        showarrow=False,
                        font=dict(color='black' if abs(value) < 10 else 'white')
                    )
                )
        fig = go.Figure()

        fig.add_trace(go.Heatmap(
            z=pivot_table.values,
            x=['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun', 'Jul', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'],
            y=pivot_table.index,
            colorscale=[
                [0, 'darkred'],
                [0.49, '#ff6666'],
                [0.5, 'white'],
                [0.51, '#99ff99'],
                [1, 'darkgreen']
            ],
            colorbar_title="Returns (%)",
            showscale=False,
            zmin=-20,
            zmax=20,
        ))

        fig.add_trace(go.Heatmap(
            z=monthly_averages,
            x=['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun', 'Jul', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'],
            y=['Avgs: '],
            showscale=False,
            text=monthly_averages,
            texttemplate='%{z:.2f}%',
            yaxis='y2',
            xaxis='x2',
        ))

        fig.show()

tickers = ['VCB', 'TPB', 'MBB', 'VIB', 'TCB', 'VPB', 'ACB', 'BID', 'CTG', 'EIB']

data = client.Fetch_Trading_Data(
    tickers=tickers,
    fields=['close'],
    adjusted=True,
    realtime=False,
    by='1d',
    from_date='2018-01-01'
).get_data()

new_SeasonalityPrice = SeasonalityPrice(data, 'VCB')
new_SeasonalityPrice.plot_seasonality()