---
name: Đóng deal (đối với phái sinh)
description: Đóng một vị thế hoặc deal phái sinh đang mở thông qua deal_id lấy từ dữ liệu vị thế phái sinh trong FiinQuantX.
---

# 7.10. Đóng deal (đối với phái sinh)

**Tên hàm:** `orderbook_der.close_deal(deal_id=...)`

**Mục đích:**
Dùng để đóng một deal phái sinh đang mở thông qua `deal_id`. Hàm này hỗ trợ người dùng tất toán một vị thế phái sinh cụ thể đã phát sinh trước đó.

**Tham số:**

| Tham số | Kiểu dữ liệu | Bắt buộc | Mô tả |
|---|---|---|---|
| deal_id | str hoặc int | Có | Mã định danh của deal cần đóng. `deal_id` được lấy từ các hàm vị thế phái sinh |

**Kết quả trả về (Returns):**
- Trả về object thông tin lệnh đóng deal
- Có thể gọi `.summary()` để hiển thị nhanh dữ liệu
- Dữ liệu bao gồm mã lệnh, mã phái sinh, chiều giao dịch, giá, khối lượng, mã gói vay và thời điểm tạo lệnh

**Ví dụ sử dụng:**

```python
username_dnse = 'REPLACE_WITH_YOUR_DNSE_USERNAME'
password_dnse = 'REPLACE_WITH_YOUR_DNSE_PASSWORD'

client_order = client.FiinQuantConnector(
    broker='DNSE',
    username=username_dnse,
    password=password_dnse,
    smart_otp=True
).login()

orderbook_der = client_order.get_derivative_orderbook(
    account_id='0001009212',
    loan_id='2278'
)

close_deal = orderbook_der.close_deal(deal_id="10680")
print(close_deal.summary())
```
**Output mẫu:**
```json
{
    "id": 266,
    "ticker": "41I1FB000",
    "side": "Sell",
    "price": 1805.1,
    "quantity": 1,
    "fill_quantity": 0,
    "average_price": 0.0,
    "loan_package_id": 2278,
    "initial_rate": 0.0,
    "created_date": "29/10/2025 14:43:55",
    "error": null,
    "status_code": 200
}