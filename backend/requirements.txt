# Core Web & API Framework
fastapi>=0.110.0,<1.0.0
uvicorn[standard]>=0.28.0,<1.0.0
pydantic>=2.6.0,<3.0.0
pydantic-settings>=2.2.0,<3.0.0
httpx>=0.27.0,<1.0.0
python-dotenv>=1.0.0,<2.0.0

# Testing & Async
pytest>=8.0.0,<9.0.0
pytest-asyncio>=0.23.0,<1.0.0

# Market data provider. vnai is required by Vnstock's public decorators and API-key
# entitlement/rate-limit layer; Sponsor-only packages are deliberately not runtime deps.
vnstock==4.0.7
vnai==2.5.9

# Cache & State Persistence
redis>=5.0.0

# Authentication - LOCAL verification of Supabase-minted access tokens only.
# The backend never issues sessions or stores passwords. PyJWT[crypto] pulls in
# `cryptography` for RS256/ES256 (JWKS) verification; HS256 works without it.
PyJWT[crypto]>=2.8,<3.0

# Public-API hardening: centralized HTTP rate limiter (moving-window strategy).
# In-process MemoryStorage for dev/CI; shared RedisStorage (reuses the `redis` client)
# in production. No home-grown distributed algorithm.
limits>=3.13,<6

# Durable Historical Market-Data Persistence (PostgreSQL)
# SQLAlchemy 2.0 async ORM + Alembic migrations. asyncpg is the runtime async driver;
# psycopg2-binary is the synchronous driver Alembic uses to run migrations.
sqlalchemy[asyncio]>=2.0,<2.1
alembic>=1.13,<2.0
asyncpg>=0.29,<0.32
psycopg2-binary>=2.9,<3.0

# Market-data analysis/runtime dependencies
matplotlib>=3.8.0
pandas>=2.0.0
numpy>=1.25.0
requests>=2.30.0
aiohttp>=3.9.0

# Request-local AI document extraction (no OCR/provider calls or permanent uploads).
python-multipart>=0.0.22,<1
pypdf>=6.16.2,<7
openpyxl>=3.1.5,<4
defusedxml>=0.7.1,<1
